import { Link, NavSection } from '../AppBar.styled';

const Navigation = () => { 
    return (
        <NavSection>
            <Link to={"/"}>Home</Link>
        </NavSection>
    );
};

export default Navigation;
